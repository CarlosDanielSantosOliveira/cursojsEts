const nome = 'Daniel';

function falanome() {
    console.log(nome);
}
falanome();
//Escopo léxico é quando a função reconhece onde esta absolutamente tudo o que ela usa, por exemplo: a variavel nome, a variavel nome
// esta fora da função, mas ela reconheceu onde estava a variável para que ela fosse utilizada.